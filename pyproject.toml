[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Finance Foundations"
authors = [{name = "G S Wone", email = "gswone@siqrs.com"}]
dynamic = ["version", "description"]
